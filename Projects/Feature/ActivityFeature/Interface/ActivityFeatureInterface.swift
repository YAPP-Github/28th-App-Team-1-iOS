//
//  ActivityFeatureInterface.swift
//  ActivityFeatureInterface
//

import Foundation

// Interface placeholder — TCA Reducer 는 자연스러운 Interface 분리가 어려워
// 본 모듈은 학습용 골격만 유지한다. 실제 분리 가치는 Client (UserClient 등) 에서.
public enum ActivityFeaturePackageInfo {
    public static let identifier = "ActivityFeatureInterface"
}
